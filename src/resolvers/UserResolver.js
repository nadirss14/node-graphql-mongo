export default {
	getUsers: () => {
		return ['myPortfolio'];
	},
	getUser: args => {
		// const portfolio = myPortfolio.filter(port => port._id === args.id);
		// return portfolio.pop();
		return 'myPortfolio';
	},
};
